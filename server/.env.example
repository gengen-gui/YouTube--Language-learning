# Server config. Copy to .env and adjust as needed.
PORT=8787
# CHANGE THIS in production. Used to sign JWT tokens.
JWT_SECRET=please-change-this-secret
# SQLite database file path (relative to server/)
DB_FILE=data.db
