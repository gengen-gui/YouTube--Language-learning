# Used by `docker compose`. Copy to `.env` and edit.
#
# Generate a strong secret with:
#   openssl rand -hex 32

JWT_SECRET=please-change-me-to-a-long-random-string

# Web UI port on your host machine
WEB_PORT=8080
